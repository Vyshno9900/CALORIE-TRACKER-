{
  "name": "vyshno-ai",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "client-install": "npm install --prefix ./src",
    "client-build": "npm run build --prefix ./src"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "node-fetch": "^3.3.2"
  }
}
